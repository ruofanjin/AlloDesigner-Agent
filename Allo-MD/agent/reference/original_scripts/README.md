# 历史脚本隔离区

本目录保存原项目中的命令笔记和分析脚本，目的仅是审计来源。它们被刻意设为不可执行，且没有进入 `manifests/stages.json`。

已知问题包括：旧机器绝对路径、交互式组号/输入不足、`gmx` alias、裸命令提示符和中文标题、过时或不存在的 MDP 名称、把独立常规分支写成串联、SPIB 路径漂移、MDpocket 帧号偏移，以及指向错误原子编号分支的历史内容。自主运行只能使用 `agentctl`、`stages/` 和 `tools/` 中的整理版。
