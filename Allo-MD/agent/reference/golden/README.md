# 数值基线状态

本版本没有把任意旧轨迹片段包装成跨平台 numerical golden，也没有在整理过程中重跑短程模拟。原因是原 eq1 使用随机种子，且 GPU、MPI 分解与 SIMD 会产生浮点差异；未经定义的逐字节比较会造成虚假的失败或通过。

已冻结并自动检查的是输入哈希、体系/选择原子数、MDP 步数和 PLUMED 结构。目标团队若需要数值回归，应在已验收的平台上另建一套自洽的短程 TPR、轨迹帧、能量/CV 输出、软件版本和容差，并在新的 bundle 版本中加入，不得只复制单个 COLVAR/HILLS 文件作为“golden”。
