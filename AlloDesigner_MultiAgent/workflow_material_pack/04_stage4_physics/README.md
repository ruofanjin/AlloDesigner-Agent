# 04_stage4_physics

**Demo**：链接 `glp1r_distance_1ns_demo_001`（需含 `distance_metad_1ns.xtc/.gro`）与 MultiAgent `stage_result.json`。  

**重算**：配置 `site.env`、GMX+PLUMED，用 Allo-MD profile；正式 ~200 ns 为可选，非 demo 必需。
