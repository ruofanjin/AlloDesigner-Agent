# 06_stage6_vs

必备：

- `job_pack/`：receptor、pocket box、Vina/Glide 操作说明、`ingest_return.py`  
- 回传：`return.json` + `ranked_candidates.csv`（真对接或 seeded）  

口径：作业单 + 回传接口可 seeded；不可把 seeded 分数说成真对接或有效配体。
